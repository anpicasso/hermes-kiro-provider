# Kiro login companion installed

This command plugin is inert until the `kiro-provider` component is installed.

```bash
hermes plugins install anpicasso/hermes-plugin-kiro/provider --no-enable
hermes plugins enable kiro
```

Or run the repository's one-line installer to install and validate both components together.